<?php

function getAllCountries(){
  echo "works";
  // require "../../../db.php";
  // $arr= array();
  // $query = "SELECT DISTINCT(country_code) FROM schools ";
  // $result = pg_query($dbconn,$query) or die('Query failed: ' . pg_last_error());
  // while ($line = pg_fetch_object($result)) {
  //    array_push($arr, $line->country_code);
  // }
  // return $arr;

}


 ?>
